# Verifier audit log — feature 10 (Filesystem, uploads, shell, MCP)

Verifier session: f37c34ae1b984ca091c72a49c5b242b0
Callback target: dbeeec369cb84db78ba88f0ec2a07b83
HEAD at start: 0036f7c1 docs(routing): resolve spec internal inconsistencies (branch v1-rebuild)

## 2026-05-07T20:19 — read assignment + runbook

- Read `.v1-ship/runbooks/verifier.md` in full.
- Located feature-10 findings at `V1_FEATURE_AUDIT.md:415-488`.
- Four numbered findings under "Hard gaps" + "Wrong":
  1. `bearings_dir/` package entirely absent (5 modules).
  2. `cli/gc.py` missing.
  3. `bearings_mcp.py` incomplete (missing `bash`, `dir_init`, `get_tool_output`).
  4. `web/routes/history.py` is a DB full-text search route (wrong shape).

## 2026-05-07T20:20 — verify finding 1: bearings_dir/ absent

- `Glob src/bearings/bearings_dir/**/*` → no files found. Package
  directory does not exist.
- `web/routes/pending.py:32-71` confirms direct `tomllib.load` /
  `tomli_w.dump` against `.bearings/pending.toml` with **no** atomic
  tempfile-rename, no agent-layer separation. Lines 56-58 read raw,
  lines 70-71 do `path.open("wb")` then `tomli_w.dump` directly —
  not POSIX-atomic.
- `git log --first-parent v1-rebuild -- src/bearings/bearings_dir/`
  returns commits only from the orphan-history side (v0.17.x) — no
  bearings_dir/ commit on v1-rebuild branch. The package was never
  ported.
- Spec anchor: `docs/architecture-v1.md:277-293` (§1.1.6) lists 5
  modules: contract.py / io.py / lifecycle.py / onboarding.py /
  pending.py.
- Verdict: **STILL_OPEN**.

## 2026-05-07T20:21 — verify finding 2: cli/gc.py missing

- `Glob src/bearings/cli/*.py` → app.py, serve.py, todo.py, _todo_io.py,
  __init__.py. **No gc.py.**
- `cli/app.py:21` docstring: *"Stubs for serve / init / window / send /
  here / pending / gc are deferred per arch §1.1.1 + behavior doc"*.
  The stub comment confirms gc is unimplemented.
- `cli/app.py:75-76` only registers `todo_cli.build_subparser` and
  `serve_cli.build_subparser` — no `gc_cli.build_subparser`.
- Bootstrap message line 46-48: *"todo + serve subcommands wired; init
  / window / send / here / pending / gc land in subsequent items"* —
  v1.0.0 still ships the deferred-notice text.
- Spec anchor: `docs/architecture-v1.md:69` (§1.1.1) — `cli/gc.py` →
  `bearings gc uploads`. Behavior doc `docs/behavior/bearings-cli.md:
  136-156` documents the full surface (`--retention-days`, `--dry-run`,
  exit codes, stdout shape).
- Verdict: **STILL_OPEN**.

## 2026-05-07T20:22 — verify finding 3: bearings_mcp.py incomplete

- File at `src/bearings/agent/bearings_mcp.py` (1-212).
- Sole tool exposed: `close_session` (line 134-178).
- `build_bearings_mcp_server` line 191-194: `tools=[make_close_session_tool(deps)]`
  — single-element list. The docstring at 184-186 acknowledges *"Today
  the server exposes only close_session; future tools register the same
  way"* — no future tools registered yet.
- `Grep -r "bearings__bash|bearings__dir_init|bearings__get_tool_output"
  src/bearings` → zero matches. None of the three tools the v0.17.x MCP
  surface advertises are present in v1.
- Verdict: **STILL_OPEN**. The audit's "fiction in three places"
  framing holds — the v1 agent-facing MCP shape advertises tools that
  do not exist.

## 2026-05-07T20:23 — verify finding 4: history.py is wrong module

- `web/routes/history.py:1-188` reads as a DB full-text search route:
  `GET /api/history/search?q=` with two-surface LIKE search across
  `sessions` and `messages` tables (lines 100-167), capped by
  `HISTORY_SEARCH_RESULT_CAP`.
- No `history.jsonl` reader anywhere: `Grep "history.jsonl" src/bearings`
  returns no files.
- Spec anchor: `docs/architecture-v1.md:252` — `web/routes/history.py`
  should be the `history.jsonl` reader.
- Module-namespace collision: the audit calls out joint landing with
  feature-5 sidebar search — the search functionality must be renamed
  to `search.py` (or moved into `sessions.py`) and a real jsonl reader
  must take the `history.py` slot.
- Verdict: **STILL_OPEN**.

## Summary

- 4/4 findings verified STILL_OPEN.
- 0 FIXED_SINCE_AUDIT.
- 0 SUPERSEDED.
- 0 extra findings discovered during verification.
- Output file: `.v1-ship/verified/feature-10.jsonl`.
